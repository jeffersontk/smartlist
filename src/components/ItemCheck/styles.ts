import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#c4c4c4",
    backgroundColor: "transparent",
    width: "100%",
    marginBottom: 12,
  },
});
